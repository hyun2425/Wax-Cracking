# 프론트엔드 보조 도구 안내

프론트엔드 작업 규칙은 `AGENTS.md`를 기준으로 합니다.

작업 전에 아래 파일을 먼저 확인하세요.

```text
AGENTS.md
```

핵심 요약:

- `src/app/page.tsx`는 메인 페이지입니다.
- `src/app/layout.tsx`는 폰트와 메타데이터를 관리합니다.
- `src/app/globals.css`는 전역 스타일을 관리합니다.
- 작업 후 `npm run lint`와 `npm run build`를 실행합니다.
